/* ==========================================================================
   YOUR SETTINGS — this is the only file you need to edit.

   Put your own Google Tag Manager container ID between the quotes below,
   save the file, and every page in this course will load your container.

   Find the ID at the top right of your Tag Manager workspace.
   It looks like GTM-A1B2C3D.

   Leave it empty and the pages still work — you can then paste an ID into
   the box under the dataLayer panel on any page instead.
   ========================================================================== */

window.GTM_COURSE_CONFIG = {

    containerId: '',

    /* Optional. Set to true to hide the "paste your container ID" box on every
       page, once you have set the ID above and do not want students changing it. */
    hideConnectBox: false

};
